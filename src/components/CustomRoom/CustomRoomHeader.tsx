import React from "react";

const CustomRoomHeader: React.FC = () => {
  // Header/status da sala custom
  return (
    <header>
      <h1>Status da Sala</h1>
      {/* Nome da sala, modo, timer, etc */}
    </header>
  );
};

export default CustomRoomHeader;
